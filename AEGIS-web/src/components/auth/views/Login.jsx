import { useState } from 'react'
import { route } from '../../shared/presentation'

export default function Login({ error, success }) {
    const [showPassword, setShowPassword] = useState(false)
    return <main className="auth-page"><section className="left-panel"><div className="security-wrapper"><div className="security-icon"><i className="fa-solid fa-lock" /></div><h1>Acceso Seguro</h1><p>Tu cuenta está protegida por encriptaciones y múltiples sistemas de seguridad y autenticación.</p><div className="security-card"><h3>Indicadores de Seguridad</h3>{['256-bit SSL encriptación', 'Alertas de inicio de sesión', 'Monitoreo en sesión activa'].map((item) => <div className="security-item" key={item}><i className="fa-solid fa-check" /><span>{item}</span></div>)}</div></div></section><section className="right-panel"><div className="form-container"><a href={route('/home')} className="back-home">← Volver al inicio</a><h2>Bienvenido de nuevo</h2>{error && <div className="auth-alert auth-alert--error">{error}</div>}{success && <div className="auth-alert auth-alert--success">{success}</div>}<form method="POST" action={route('/login')}><div className="input-group"><label htmlFor="email">Correo electrónico</label><input type="email" id="email" name="email" placeholder="Alex@gmail.com" required /></div><div className="input-group"><label htmlFor="password">Contraseña</label><div className="password-wrapper"><input type={showPassword ? 'text' : 'password'} id="password" name="password" placeholder="Contraseña" required minLength="6" /><button type="button" className="eye-icon" onClick={() => setShowPassword((value) => !value)} aria-label="Mostrar contraseña"><i className="fa-regular fa-eye" /></button></div></div><button type="submit" className="login-btn"><i className="fa-solid fa-lock" /> Iniciar Sesión</button></form><p className="register-hint">¿No tienes cuenta? <a href={route('/register')}>Regístrate</a></p></div></section></main>
}